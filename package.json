{
  "name": "daemon",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ daemon",
    "create-local": "graph create --node http://localhost:8020/ daemon",
    "remove-local": "graph remove --node http://localhost:8020/ daemon",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 daemon",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.83.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
